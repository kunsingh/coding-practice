package LongestCommonSubSequenceBased;

/**
 * A: heap
 * B: pea
 *
 * A- > B => 2 deletion from A(h,p) and one insertion to A(p)
 *
 * len(A) - lcs = deletion
 * len(B) - lcs = insertion
 */
public class MinimumInsertionAndDeletion {
}
